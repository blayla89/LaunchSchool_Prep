// 1. Determine the length of the string "These aren't the droids you're looking for.".

let length = "These aren't the droids you're looking for.".length; 
console.log(length);

// String.prototype.length - contains the length of the string in UTF-16 code units
  // pre-computed value, no () because it is not a function or method and thus cannot be invoked